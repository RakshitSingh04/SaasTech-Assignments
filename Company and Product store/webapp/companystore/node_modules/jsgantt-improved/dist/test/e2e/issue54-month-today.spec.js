"use strict";
/**
 * E2E tests for issue #54: Month view — today line hidden and month header
 * missing when the max date falls on the 1st of a month.
 *
 * Root cause (suspected): getMaxDate() in month format uses:
 *   while (vDate.getDate() > 1) vDate.setDate(vDate.getDate() + 1);
 *   vDate.setDate(vDate.getDate() - 1);
 * When vDate is already the 1st of a month the while never runs, then
 * setDate(0) rolls back to the last day of the *previous* month — so
 * a task ending August 1 causes vMaxDate = July 31, hiding August.
 *
 * Test cases (no Date mocking needed):
 *
 *  1. "August header" — tasks span Jan–Aug 1 2025.
 *     Bug: vMaxDate = July 31  → August column missing.
 *     Fix: vMaxDate = August 31 → August column present.
 *
 *  2. "today line" — tasks span Jan 2026 → April 1 2026 (today = April 13 2026).
 *     Bug: vMaxDate = March 31 < today (Apr 13) → today line hidden.
 *     Fix: vMaxDate = April 30 ≥ today            → today line visible.
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var test_1 = require("@playwright/test");
// Use the base URL from the environment (or default) and append demo.html.
// We build the full URL here because DEMO_URL ('demo.html') is a relative path
// and Playwright resolves it relative to the base URL *without* a trailing
// slash, which can produce a 404 (e.g. https://host/project → https://host/demo.html).
function demoUrl() {
    var _a;
    var base = ((_a = process.env.JSGANTT_E2E_BASE_URL) !== null && _a !== void 0 ? _a : 'http://localhost:8080').replace(/\/$/, '');
    return "".concat(base, "/demo.html");
}
/** Build a gantt in month format by injecting into the live demo page. */
function mountMonthGantt(page, startMM, startDD, startYYYY, endMM, endDD, endYYYY) {
    return __awaiter(this, void 0, void 0, function () {
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: 
                // Ensure the demo page has rendered at least once before we overwrite it
                return [4 /*yield*/, page.waitForSelector('#embedded-Gantt', { timeout: 15000 })];
                case 1:
                    // Ensure the demo page has rendered at least once before we overwrite it
                    _a.sent();
                    return [4 /*yield*/, page.evaluate(function (_a) {
                            var startMM = _a.startMM, startDD = _a.startDD, startYYYY = _a.startYYYY, endMM = _a.endMM, endDD = _a.endDD, endYYYY = _a.endYYYY;
                            var container = document.getElementById('embedded-Gantt');
                            if (!container)
                                throw new Error('#embedded-Gantt not found');
                            container.innerHTML = '';
                            var g = new window['JSGantt'].GanttChart(container, 'month');
                            g.setOptions({ vFormat: 'month', vFormatArr: ['Month'] });
                            // Default date input format is yyyy-mm-dd
                            var pad = function (n) { return String(n).padStart(2, '0'); };
                            var fmt = function (y, m, d) { return "".concat(y, "-").concat(pad(m), "-").concat(pad(d)); };
                            g.AddTaskItem(new window['JSGantt'].TaskItem(1, 'Task A', fmt(startYYYY, startMM, startDD), fmt(endYYYY, endMM, endDD), '', '', 0, 'Me', 0, 1, 0, 1, '', '', '', g));
                            g.Draw();
                        }, { startMM: startMM, startDD: startDD, startYYYY: startYYYY, endMM: endMM, endDD: endDD, endYYYY: endYYYY })];
                case 2:
                    _a.sent();
                    return [4 /*yield*/, page.waitForSelector('#embedded-Gantt .gcharttable', { timeout: 10000 })];
                case 3:
                    _a.sent();
                    return [4 /*yield*/, page.waitForTimeout(300)];
                case 4:
                    _a.sent();
                    return [2 /*return*/];
            }
        });
    });
}
test_1.test.describe('Issue #54 — Month view: today line and month header', function () {
    test_1.test.beforeEach(function (_a) {
        var page = _a.page;
        return __awaiter(void 0, void 0, void 0, function () {
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: return [4 /*yield*/, page.goto(demoUrl(), { waitUntil: 'networkidle', timeout: 25000 })];
                    case 1:
                        _b.sent();
                        return [2 /*return*/];
                }
            });
        });
    });
    (0, test_1.test)('August column header is visible when task ends on Aug 1', function (_a) {
        var page = _a.page;
        return __awaiter(void 0, void 0, void 0, function () {
            var headerTexts;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: 
                    // Task Jan 1 2025 → Aug 1 2025
                    // Bug: vMaxDate rolls back to July 31 → August header missing
                    return [4 /*yield*/, mountMonthGantt(page, 1, 1, 2025, 8, 1, 2025)];
                    case 1:
                        // Task Jan 1 2025 → Aug 1 2025
                        // Bug: vMaxDate rolls back to July 31 → August header missing
                        _b.sent();
                        return [4 /*yield*/, page.evaluate(function () {
                                var _a;
                                var headerTable = document.querySelector('#embedded-Gantt .gcharttableh');
                                if (!headerTable)
                                    return [];
                                var rows = headerTable.querySelectorAll('tr');
                                var lastRow = rows[rows.length - 1];
                                return Array.from((_a = lastRow === null || lastRow === void 0 ? void 0 : lastRow.querySelectorAll('td')) !== null && _a !== void 0 ? _a : []).map(function (td) { return td.innerText.trim(); });
                            })];
                    case 2:
                        headerTexts = _b.sent();
                        (0, test_1.expect)(headerTexts.some(function (t) { return /aug/i.test(t); }), "Expected August in month header, got: ".concat(JSON.stringify(headerTexts))).toBe(true);
                        return [2 /*return*/];
                }
            });
        });
    });
    (0, test_1.test)('today line is rendered when task ends on the 1st and today is within that month', function (_a) {
        var page = _a.page;
        return __awaiter(void 0, void 0, void 0, function () {
            var todayLineExists;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: 
                    // Today = 2026-04-13. Task ends April 1 2026.
                    // Bug: vMaxDate = March 31, 2026 < today (Apr 13) → today line hidden
                    // Fix: vMaxDate = April 30, 2026 ≥ today          → today line visible
                    return [4 /*yield*/, mountMonthGantt(page, 1, 1, 2026, 4, 1, 2026)];
                    case 1:
                        // Today = 2026-04-13. Task ends April 1 2026.
                        // Bug: vMaxDate = March 31, 2026 < today (Apr 13) → today line hidden
                        // Fix: vMaxDate = April 30, 2026 ≥ today          → today line visible
                        _b.sent();
                        return [4 /*yield*/, page.evaluate(function () {
                                return document.querySelector('#embedded-Gantt .gCurDate') !== null;
                            })];
                    case 2:
                        todayLineExists = _b.sent();
                        (0, test_1.expect)(todayLineExists, 'Today line (.gCurDate) should be rendered').toBe(true);
                        return [2 /*return*/];
                }
            });
        });
    });
});
//# sourceMappingURL=issue54-month-today.spec.js.map