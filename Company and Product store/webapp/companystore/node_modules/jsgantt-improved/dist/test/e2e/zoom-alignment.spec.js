"use strict";
/**
 * E2E tests for issue #23: bar misalignment at non-100% browser zoom.
 *
 * Root cause: table-layout:auto gives columns fractional pixel widths at
 * non-100% zoom. As columns accumulate, bars drift away from their date.
 *
 * Fix: table-layout:fixed + explicit JS cell widths (already applied).
 *
 * Test strategy:
 *   - Apply CSS zoom to #embedded-Gantt (equivalent to browser-level zoom)
 *   - At each zoom level, assert bars scale proportionally with zoom
 *     (viewportLeft ≈ styleLeft × zoom ± tolerance)
 *   - Assert column widths are uniform within each zoom level
 *     (no fractional-px spread from table-layout:auto)
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var test_1 = require("@playwright/test");
var gantt_page_1 = require("./gantt.page");
var ZOOM_LEVELS = [0.75, 0.8, 1.0, 1.25, 1.5];
/** Maximum allowed drift between expected and actual bar position (px). */
var DRIFT_TOLERANCE_PX = 3;
/** Maximum allowed spread between widest and narrowest column at the same zoom (px). */
var COL_SPREAD_TOLERANCE_PX = 1;
test_1.test.describe('Issue #23 — bar alignment at non-100% zoom', function () {
    var _loop_1 = function (zoom) {
        var label = "".concat(Math.round(zoom * 100), "%");
        (0, test_1.test)("bars are correctly aligned at ".concat(label, " zoom"), function (_a) {
            var page = _a.page;
            return __awaiter(void 0, void 0, void 0, function () {
                var gantt, bars, _i, bars_1, bar, expectedLeft, drift;
                return __generator(this, function (_b) {
                    switch (_b.label) {
                        case 0:
                            gantt = new gantt_page_1.GanttPage(page);
                            return [4 /*yield*/, gantt.goto(gantt_page_1.DEMO_URL)];
                        case 1:
                            _b.sent();
                            return [4 /*yield*/, gantt.applyZoom(zoom)];
                        case 2:
                            _b.sent();
                            return [4 /*yield*/, gantt.getBarMetrics(8)];
                        case 3:
                            bars = _b.sent();
                            (0, test_1.expect)(bars.length, 'should find rendered task bars').toBeGreaterThan(0);
                            for (_i = 0, bars_1 = bars; _i < bars_1.length; _i++) {
                                bar = bars_1[_i];
                                expectedLeft = bar.styleLeft * zoom;
                                drift = Math.abs(bar.viewportLeft - expectedLeft);
                                (0, test_1.expect)(drift, "".concat(bar.id, " at zoom ").concat(label, ": viewportLeft=").concat(bar.viewportLeft, " expected\u2248").concat(expectedLeft.toFixed(1))).toBeLessThanOrEqual(DRIFT_TOLERANCE_PX);
                            }
                            return [2 /*return*/];
                    }
                });
            });
        });
        (0, test_1.test)("column widths are uniform at ".concat(label, " zoom"), function (_a) {
            var page = _a.page;
            return __awaiter(void 0, void 0, void 0, function () {
                var gantt, baseCols, baseColWidth, cols, widths, spread, expectedWidth, avgWidth;
                var _b, _c;
                return __generator(this, function (_d) {
                    switch (_d.label) {
                        case 0:
                            gantt = new gantt_page_1.GanttPage(page);
                            return [4 /*yield*/, gantt.goto(gantt_page_1.DEMO_URL)];
                        case 1:
                            _d.sent();
                            return [4 /*yield*/, gantt.getColumnMetrics(5)];
                        case 2:
                            baseCols = _d.sent();
                            baseColWidth = (_c = (_b = baseCols[0]) === null || _b === void 0 ? void 0 : _b.viewportWidth) !== null && _c !== void 0 ? _c : 39;
                            return [4 /*yield*/, gantt.applyZoom(zoom)];
                        case 3:
                            _d.sent();
                            return [4 /*yield*/, gantt.getColumnMetrics(20)];
                        case 4:
                            cols = _d.sent();
                            (0, test_1.expect)(cols.length, 'should find header columns').toBeGreaterThan(0);
                            widths = cols.map(function (c) { return c.viewportWidth; });
                            spread = Math.max.apply(Math, widths) - Math.min.apply(Math, widths);
                            (0, test_1.expect)(spread, "column width spread at ".concat(label, " zoom (min=").concat(Math.min.apply(Math, widths).toFixed(2), " max=").concat(Math.max.apply(Math, widths).toFixed(2), ")")).toBeLessThanOrEqual(COL_SPREAD_TOLERANCE_PX);
                            expectedWidth = baseColWidth * zoom;
                            avgWidth = widths.reduce(function (a, b) { return a + b; }, 0) / widths.length;
                            (0, test_1.expect)(Math.abs(avgWidth - expectedWidth), "avg col width ".concat(avgWidth.toFixed(2), " should be \u2248 baseCol(").concat(baseColWidth, ") \u00D7 zoom(").concat(zoom, ") = ").concat(expectedWidth.toFixed(2))).toBeLessThanOrEqual(1);
                            return [2 /*return*/];
                    }
                });
            });
        });
    };
    for (var _i = 0, ZOOM_LEVELS_1 = ZOOM_LEVELS; _i < ZOOM_LEVELS_1.length; _i++) {
        var zoom = ZOOM_LEVELS_1[_i];
        _loop_1(zoom);
    }
});
//# sourceMappingURL=zoom-alignment.spec.js.map